# TOE-SYLVA 发展战略与方向（DEVELOPMENT DIRECTIONS）

> **版本**：v1.0 ｜ **日期**：2026-08-06
> **制定依据**（三路输入，均可核查）：
> ① 全库综合分析报告（2026-08-06，对 `D:\TOE-SYLVA-pull` 的只读实测：目录统计、git 历史、千份随机抽样、治理文档精读）；
> ② 千界花园专家组评议两份——《数学基础强化方案评议》（committee 模式，形式化验证/拓扑/数论/代数几何四专家）与《新发展方向论证评议》（debate 模式，跨学科/广义相对论/量子力学/机器学习四专家）；
> ③ arXiv 自动形式化文献扫描（2024–2026，12 条文献，编号经核验端点 12/12 有效）。
> **地位**：本文件是仓库级战略文件，与 `ROADMAP_V6.md`（版本路线）、`framework/proof_status.md`（证明治理）互补；凡口径冲突，以本文件第五节确立的度量口径为准。

---

## 一、现状诚实评估

### 1.1 继承审计传统

本文件继承而非新开仓库的诚实性传统。这一传统的已有标志：

- `framework/proof_status.md`（v1.1）——框架内首个诚实的证明率登记，自认历史"证明率 100%"口径错误；
- AI 幻觉专项审计（2026-07/08）——计数几何方向识别并删除 15 条虚构主张（机器可读日志 `data/deleted_fabricated_claims.json`），产出"失败模式分类学 + 可验证写作协议"（`papers/AI_FAILURE_TO_VERIFIABLE_PROTOCOL.md`），被远程 README 认定为整个项目"唯一真正的新贡献"；
- 四轮质量工程（2026-08）——1,943 个 arXiv ID 全量核验、25 个失败验证脚本清零（修真实物理 bug 而非放宽断言）、约 500 处引用-论述语义一致性修复、README 全面重写。

战略制定从承认实测数据开始，不从承认愿景开始。

### 1.2 平凡证明膨胀的实测图景

全库实测 **324,916 个 .lean 文件**（全部位于 `sylva_formalization/`），其构成：

| 区域 | 文件数 | 性质 |
|------|-------:|------|
| `SylvaFormalization/` 根目录平铺 | 307,907 | 其中 **287,792 个为 `SYLVA_Proven*R*M*` 批量模块**，另 20,115 个为模板模块 |
| `mathlib4_extracted/` | 7,727 | 完整 Mathlib4 源码副本（vendored，非本库原创） |
| 命名领域子目录（27 个）+ archive + 顶层测试 | ~280 | 人工策划核心所在 |

批量模块内容实录：平均每文件约 499 行、约 200 条"定理"，为循环排列的同义反复——`True := trivial`、`a + 0 = a := add_zero`、`¬False := by False.elim`、`(0:ℝ)+0=0 := by rfl`、`True ∧ True := ⟨trivial, trivial⟩`。全库定理声明估算约 **6,000 万条**，其中人工策划的非平凡证明估计 **不足 0.1%（仅数千条）**。

一个必须说清的悖论：**全库真实 sorry 为 0**（千份严格抽样复测）——不是因为证明完整，而是因为批量生成器不产出 sorry，它直接产出 `True := trivial`。"零 sorry"在本仓库不构成质量信号。

### 1.3 清算"证明率 99.5%"口径

口径混乱史必须在此清算并封存：

- v10.50 提交线自报"29,887 模块 / 508 万定理 / 证明率 88.08%"；
- v10.75–v10.79 提交标题报告"444,000→10,000,000 新证明""证明率突破 99.5%/99.9%/100%"；
- 同期 `proof_status.md` 自认真实证明率"显著低于 100%"。

三套数字并存的原因是**分母不同**：把批量平凡定理计入"已证定理"，任何口径都能逼近 100%。本文件确立唯一口径（详见第五节）：**真实证明率 = 零公理、零 postulate 的独立推导数 ÷ 声称定理总数，且人工策划模块与批量模块分开报告**。按此口径，本仓库当前的真实证明率与"99.5%"无任何关系；历史提交标题中的证明率数字自此视为已废止口径，不再在任何文档、提交信息或对外表述中引用。

### 1.4 结构性短板清单（基于证据）

1. **证明深度空心化**：物理核心的 Chern-Simons、Einstein-Cartan、Spectral Action 仍为 axiom 占位（本 pull 实测：根目录非批量 208 条 + archive 88 条 + 命名子目录 ≈ 350 条以上；README 诚实口径 478 条），Agda 六层另有约 149 个 postulate（全部 CLAIM，因 Windows 编译 OOM）。
2. **papers 与 Lean 两张皮**：`papers/` 下 0 个 .lean 文件，198 个论文目录与 30.8 万个 Lean 模块之间没有机器可追的对应关系。
3. **目录膨胀与污染**：16 个空英文目录、4 个被提交进 git 的 `.venv`（其一 1,058 个文件中 1,053 个是 venv 文件）、7,727 个 vendored Mathlib 文件、16 组 55 目录疑似重复（仅加交叉引用未合并）。
4. **期刊投稿系列仍是占位**：9 个期刊目录仅 4KB `main.md` 占位 + 12 封投稿信，无 LaTeX 源、无补充材料。
5. **学科盲区**：化学/地球/环境仅 3 目录；无经济学、心理学、社会科学，与"统一物理、生命、心智、社会四层级"的愿景不符。
6. **工程不可持续性**：36 万文件导致 `lake build`/CI OOM（proof_status P4 自述）；Agda 全量编译需 Linux ≥16GB（P3 未解）；README 本地口径（8,100 文件）与实测（32.5 万）脱节——连文档都已追不上生成器的速度。

---

## 二、数学基础强化计划

> 本节为全计划的核心，回应评议议题"如何从 478 条 axiom 与大量平凡证明走向深层定理"。详细方法论见新设论文系列 `papers/数学基础强化_系列/`，首篇《公理审计与分层：从 478 条公理到可信基础》已发布。

### 2.1 公理审计与分层方案

**问题界定**（形式化验证专家）：478 条公理已远超标准数学基础系统——ZFC 核心公理约 10 条（含替换模式）、HOL 核心推理规则约 5–8 条、CiC 基础判断屈指可数。数量本身强烈暗示**公理与定理的边界模糊**：其中可能混杂大量本可通过定义或保守扩展导出的命题。

**审计方案**：

1. **分类学**：将每条公理归入四类——本原公理（primitive）/ 定义性扩展（definitional extension，应降级为定义+定理）/ 公理模式（schema）/ 占位公理（实测发现的本仓库特有类别：结论为 `True` 的空公理，物理内容仅存在于文档字符串）。Metamath `set.mm` 的经验（Megill & Wheeler 2019）表明，约 20–30% 的原始公理模式可降级为定理；本仓库抽样已发现"可立即清偿"的实例（详见系列论文 01）。
2. **分层架构**（拓扑专家方案）：将全部公理划分为——① 逻辑-类型基础层；② 集合-序结构层；③ 分析-拓扑基础层；④ 代数-范畴工具层；外加本仓库特有的 ⑤ 物理核心层（Chern-Simons、Einstein-Cartan、Spectral Action 等）。层间只许向下依赖，禁止跨层循环依赖。
3. **独立性与保守性分析**：对每层评估其是否构成对下层的保守扩张；一致性论证按 Gödel 第二不完备性定理的要求，最终诉诸外部元理论，不在系统内部自我宣称。
4. **产出物**：机器可读的《公理登记表》（每条公理：类别、所在层、物理/数学重要性、可清偿性评估、关联 proof_status 条目）。

### 2.2 课程式形式化路线（对标 mathlib4 层级）

采纳数论专家的"课程式形式化"（curriculum-based formalization）方案：按现代研究生教材的章节顺序构建依赖图，确保每条新增定义都服务于缩短到达深层定理的路径，杜绝随机积累平凡引理。mathlib4 已验证此路线的可行性（类型类层次 + bundled structures + `aesop`/`simp`/`norm_num` 自动化将平凡证明压缩至声明式级别；素数定理已在 Lean 4 中完成形式化）。

**首条课程链（数论-分析交汇带）**：

```
Zp/Qp（p-adic 整数与 p-adic 域的拓扑代数结构）
  → Dirichlet 特征与 L-函数基础（Re(s)>1 绝对收敛性）
  → Dedekind 整环（理想分解唯一性、理想范数）
```

教材依赖图：Marcus《Number Fields》/ Neukirch《Algebraic Number Theory》→ Silverman → Diamond-Shurman。中层抽象优先于高层目标：不自建底层，复用 mathlib4 的交换代数与拓扑基础（本仓库已 vendored 的 7,727 个 Mathlib 文件将改为依赖引用，见 5.2）。

**技术配套**：引理工厂模式（lemma factory）——针对"标准估计"构建可重用 tactic 宏；数论专用自动化（Presburger 扩展、`norm_num` 数论插件），把人力从平凡证明的体力劳动转向 tactic 开发与数学结构设计。

### 2.3 从水平扩展转向纵向整合

v10 版本线的"纯量扩张"（每版新增 40 个模板模块 × 14–19 条定理）正式终止。转向纵向整合（Vertical Integration）：

- **中间层定理网络**：从基础公理到深层定理之间缺乏稳固的中间层引理网络，是"大量平凡证明"反模式的根源；每个领域模块须先建中层接口（接口层只依赖下层接口），再谈高层定理。
- **里程碑定理驱动**（拓扑专家 P0–P4 序列，作为数学侧的检验标尺）：P0 `ℝⁿ ≇ ℝᵐ`（n≠m）→ P1 Brouwer 不动点 → P2 Jordan 曲线定理 → P3 Seifert-van Kampen → P4 Mayer-Vietoris。P0–P2 陈述初等但证明足够深刻，可有效检验系统表达力；P3–P4 强制引入范畴论语言。
- **批量生成器处置**：停产；存量 28.8 万个 Proven 模块的处置见方向一（反向蒸馏）与 5.2（CI 瘦身）。

### 2.4 里程碑

**短期（6–18 个月）**
1. 完成《公理登记表》：约 478 条 axiom + 149 个 Agda postulate 全部登记、分类、分层；
2. 首批"可清偿公理"转为定理（首轮 sweep，目标 20–30 条；抽样已确认存在 `rfl` 级可证公理）；
3. 课程链第一段贯通：Zp/Qp → Dirichlet 特征与 L-函数 → Dedekind 整环，全部零 sorry、零新增公理；
4. 产生 CHSH 之外的第二个 THEOREM 级成果（proof_status 登记）；
5. 占位公理（结论为 `True` 者）全部实义化或注销；
6. 真实证明率仪表盘上线（见 5.1），废止口径完成全库清扫。

**中期（2–5 年）**
1. 复分析工具链：围道积分、留数定理的数论专用库，支撑 ζ/L-函数解析延拓；
2. 局部类域论核心断言形式化，推向 Chebotarev 密度定理局部版本；
3. 椭圆曲线任意域上群律 + ℚ 上高度理论，为 Mordell-Weil 做准备；
4. 物理核心 CLAIM→THEOREM 至少 1 例（优先 Chern-Simons 不变量定理链）；
5. 与 Mathlib 数论团队、FLT 形式化项目建立协同，不重复建造底层。

**长期（5–10 年）**
1. 直接参与或对接 FLT 形式化项目（模提升定理、半稳定提升部分）；
2. 逆向数学审计：对已达成深层定理做公理强度分析，形成数论方向逆向数学图谱，确定 478 条公理中真正的逻辑必要项；
3. 物理核心公理最小独立集 + 外部元理论一致性论证；
4. Agda 六层 postulate 清零（依赖 Linux ≥16GB 环境，P3）。

---

## 三、五大新发展方向

**收敛说明**：综合分析报告的 8 个候选方向中——候选 1（证明债务清偿）已并入第二节数学基础强化计划；候选 2（papers↔Lean 血缘图谱）、候选 3（仓库瘦身与 CI 复活）、候选 7（真实证明率仪表盘）构成第五节治理与度量；候选 4、5、6、8 与专家评议的 3 个递进方向（神经符号自动形式化 → 知识超图 → 跨学科类比生成）按下表收敛为 5 个方向：

| # | 方向 | 来源 |
|---|------|------|
| 一 | 神经符号自动形式化与平凡证明语料的反向蒸馏 | 评议方向一 + 候选 8 |
| 二 | 异构知识超图与理论涌现预测 | 评议方向二 |
| 三 | 约束化跨学科类比生成与学科盲区补齐 | 评议方向三 + 候选 6 |
| 四 | AI 幻觉审计协议的产品化推广 | 候选 5 |
| 五 | 期刊投稿系列实质化（1–2 篇打样） | 候选 4 |

### 方向一：神经符号自动形式化与平凡证明语料的反向蒸馏

- **依据**：LLM 自动形式化已具备初始能力（评议引述：Wu et al. 2022 自动形式化、GPT-f/HTPS、LeanDojo 神经定理证明）；本仓库 28.8 万个批量 Proven 模块的唯一价值是作为生成压力测试语料；2024–2026 文献扫描（12 条，见第四节）提供了完整工具链参照。
- **目标**：建立自然语言→Lean 4 的自动形式化流水线，服务数学基础强化计划（课程链命题翻译）与 papers↔Lean 血缘登记；把批量模块蒸馏为"Lean 4 平凡定理生成器基准"（miniF2F 的反面教材），以方法论论文或工具发布，把负资产转为可引用的元研究产出。
- **第一阶段行动项**：
  1. 以 FormL4（arXiv:2406.01940）与 RLMEval（arXiv:2510.25427）为评测基准完成工具选型；
  2. 在课程链开发中部署 Lean Copilot（arXiv:2404.12534）作为交互证明辅助；
  3. 从 qec_ftqc、p-adic Hodge 两个深系列抽取 50–100 条命题做 NL→Lean 翻译试点；
  4. 制定平凡定理基准的采样与标注规范（区分 `trivial`/`rfl`/`add_zero` 等模式类别）。

### 方向二：异构知识超图与理论涌现预测

- **依据**（机器学习专家）：TOE-SYLVA 的模块交互本质上是高阶的（多学科同时耦合）与动态的（随论文发表演化），普通图结构不足以建模；超图神经网络（Yadati et al. 2019；Feng et al. 2019）将 WL 框架扩展至高阶交互；消息传递 GNN 的区分能力等价于 WL 图同构测试（Xu et al. 2019 GIN），为评估模块依赖结构的可学习性提供可计算的理论上限；持久同调稳定性定理（Cohen-Steiner et al. 2007）为知识依赖结构提供鲁棒几何不变量。跨学科专家同时警示：1,000+ 模块的稠密交互面临组合稳定性危机（May 1972 复杂性-稳定性定理的类比）。
- **目标**：以"模块-论文-概念"为超边构建异构知识超图，学习 198 个论文目录与策划 Lean 模块的依赖结构，识别知识盲区、预测理论网络的相变点与潜在新兴交叉领域。
- **第一阶段行动项**：
  1. 在血缘图谱基础设施（见 5.3）之上定义超图 schema；
  2. 对 182 个非空论文目录建立静态超图基线；
  3. 用 GIN/WL 上限评估模块聚类的可区分性；
  4. 选取 2–3 个模型预测的候选交叉点提交人工评审（闭环验证）。

### 方向三：约束化跨学科类比生成与学科盲区补齐

- **依据**：科学史上的重大突破常源于跨领域类比（机器学习专家）；LLM 经检索增强生成（RAG）注入论文上下文、并附加形式化约束（生成假设须在相关模块并集语言中可表述，经 Lean 类型检查或 SMT 求解器过滤），可把类比推理从认知隐喻转化为可计算流程；本仓库化学/地球/环境仅 3 目录，与四层级愿景不符，而 v10 模板生成器已被证明能低成本产出结构化综述骨架（SYLVA_Geobiology、SYLVA_Astrobiology 等已有 Lean 侧占位模块可对接）。
- **目标**：用"生成器起骨架、人工+文献核验填血肉、形式化过滤器兜底"的流程，将 3 个短板学科（化学、地球科学、生命科学交叉）补齐到可用规模；幻觉风险由方向四的审计协议全程兜底。
- **第一阶段行动项**：
  1. 选一个化学主题打样：生成骨架 → 逐条检索核验 → 幻觉审计协议核查；
  2. 定义"生成-核验"双人流程与退出标准（未通过核验的句子不得入库）；
  3. 对接已有 SYLVA_Geobiology / SYLVA_Astrobiology Lean 占位模块，同步登记血缘。

### 方向四：AI 幻觉审计协议的产品化推广

- **依据**：`papers/AI_FAILURE_TO_VERIFIABLE_PROTOCOL.md` 被远程 README 认定为整个项目"唯一真正的新贡献"；已有机器可读日志（`data/deleted_fabricated_claims.json`）与校验脚本（`scripts/verify_honest_repo.py`）；2026 年文献确认"语义幻觉"（类型检查通过 ≠ 命题忠实表达数学意图）是自动形式化前沿痛点（Lean Atlas，arXiv:2604.16347），本仓库的协议恰是对该痛点的先行实践。
- **目标**：把计数几何方向的审计协议推广到全部 182 个非空目录（每目录一轮语义一致性核查）；将失败模式分类学、检查清单与机器可读日志格式整理为可外部复用的协议产品。
- **第一阶段行动项**：
  1. 按引用密度排定 182 个目录的审计顺序；
  2. 把 15 条删除记录的失败模式分类学泛化为通用检查清单；
  3. 扩展 `verify_honest_repo.py`，输出机器可读审计日志；
  4. 与方向一工具链联动：LLM 初审 + 人工复核的双层流程。

### 方向五：期刊投稿系列实质化（1–2 篇打样）

- **依据**：9 个期刊目录（PRD/PRX/JFM/Nature 系/NeurIPS 等）目前仅 4KB `main.md` 占位 + 投稿信；而 qec_ftqc（126 文件、12 篇系列论文、每篇 8+ 张数值验证图，全库最扎实的图证链）与 SYK-cMERA（已有 99.7% 数值吻合声明 + 对标验证脚本）具备最接近成稿的素材；原 `submission_covers/` 12 封投稿信已写好（v7.47 P2-2 已归并到各期刊目录 `cover_letter.md`）。
- **目标**：选 1 篇补齐 LaTeX 源、补充材料与审稿回复预案，达到真实投稿状态；以投稿标准反向校验全库方法论。风险明示：素材仍需同行级重审。
- **第一阶段行动项**：
  1. qec_ftqc 与 SYK-cMERA 二选一，完成同行级重审；
  2. 补齐 LaTeX 源与补充材料；
  3. 按 proof_status 口径明确标注探索性陈述与已验证断言的边界；
  4. 制定审稿意见回复预案。

---

## 四、自动形式化工具链

> 基于 2024–2026 文献扫描的 12 条真实文献（编号全部经核验端点验证可达）。选型原则：**以研究级基准选型，不以 curated 基准自报成绩**。

### 4.1 工具选型（按职能分组）

| 职能 | 文献 | arXiv | 对本仓库的用途 |
|------|------|-------|----------------|
| 交互证明辅助 | Lean Copilot | 2404.12534 | 课程链开发的 LLM 副驾驶；机器可检查性天然消除证明幻觉 |
| 过程驱动基准 | Process-Driven Autoformalization（FormL4） | 2406.01940 | 跟踪 Lean 4 快速演进的选型基准 |
| 训练语料 | Lean Workbook | 2406.03847 | NL→Lean 问题集构建方法，用于方向一的翻译试点 |
| 意图形式化评估 | LLM-driven User-Intent Formalization | 2406.09757 | 评估"命题是否忠实表达意图"的框架（验证感知语言场景） |
| 泛化能力实测 | Autoformalization in the Wild | 2502.12065 | 真实数学定义上的落差警示：按"野生"文本而非教科书例句评估 |
| 统一框架 | Towards a Common Framework for Autoformalization | 2509.09810 | 流水线总体架构参照 |
| 多智能体架构 | MASA | 2510.08988 | 多智能体分工形式化框架——直接借鉴对象（见 4.2） |
| 研究级评测 | RLMEval | 2510.25427 | 面向真实 Lean 项目的评测套件；本仓库流水线须以其为验收标准 |
| LLM 输出核验 | Autoformalization of LLM-generated Outputs | 2511.11829 | "LLM 产出是否正确"的形式化验证路径，与方向四联动 |
| 语义幻觉警示 | Lean Atlas | 2604.16347 | 类型检查 ≠ 命题忠实；流水线必须含"命题-意图对齐"元层 |
| 鲁棒性警示① | Paraphrase-Induced Failures | 2604.23135 | 前沿模型对同义改写不鲁棒；单一范式评测高估真实能力 |
| 鲁棒性警示② | Robustness of Proof Autoformalization | 2606.14867 | 证明级形式化必须处理非规范化的真实证明文本 |

### 4.2 MASA 多智能体架构借鉴

MASA（arXiv:2510.08988）提出由多个 LLM 智能体分工将自然语言命题转换为形式化表示的框架，与千界花园的 panels 群智评议架构同构（角色划分 + 一致性校验）。借鉴设计：

```
论文命题抽取智能体  →  形式化翻译智能体  →  Lean 类型检查（编译器硬门）
      →  语义对齐复核智能体（防语义幻觉，Lean Atlas 警示）  →  proof_status 登记
```

- **角色划分**：抽取 / 翻译 / 校验 / 复核四类智能体，对应 panels 的 contributor / chair / reviewer 分工；
- **一致性校验**：翻译结果须通过编译器硬门 + 语义复核软门，双重门禁后方可进入血缘图谱登记；
- **与人协同**：所有自动形式化产物进入仓库前经人工抽查（初期抽查率 100%，随 RLMEval 成绩提升逐步放宽）。

### 4.3 风险与警示（文献明示）

1. **语义幻觉是当前前沿痛点**（Lean Atlas / Paraphrase 两篇 2026 年工作）：类型检查通过 ≠ 命题忠实。本仓库 478 条 axiom 体系尤其需要"命题—意图对齐"的元层验证——这与方向四的审计协议互为表里。
2. **鲁棒性评测缺口**：现有评测集中于 curated 数据，真实文献文本上的自动形式化仍脆弱（2502.12065、2606.14867）。对平凡证明的自动清理不可盲目信任工具输出。
3. **curated 成绩 ≠ 研究级影响力**（RLMEval，2510.25427）：本仓库工具链的成效以研究级基准与外界可复现结果报告，不自报内部口径。

---

## 五、治理与度量

### 5.1 真实证明率仪表盘

- **唯一定义**：真实证明率 = 零公理、零 postulate 的独立推导定理数 ÷ 声称定理总数（proof_status.md 已给出定义但无执行，此处补上执行）。
- **分层报告**：人工策划模块与批量模块分开计数、分开报告，禁止合并出"总证明率"。
- **实现**：写一个 10 行级统计脚本接入 CI，每次提交自动更新 `framework/DASHBOARD.md`；一次性终结 88.08% / 99.5% / 99.9% / 100% 并存的口径混乱——成本极低，治理价值最高。
- **口径纪律**：提交标题与文档中禁止出现未经该脚本复算的证明率数字；`framework/` 下 60 余份元文档（DASHBOARD/STATISTICS/ProgressHeatmap 等）逐步统一到本口径。

### 5.2 CI 瘦身

目标：让 `lake build` + `agda --compile` 重新可在 CI 运行（这是一切后续验证工作的前提）。

1. 4 个 `.venv` 目录移出 git（加入 .gitignore，历史遗留另行清理）；
2. 7,727 个 `mathlib4_extracted/` vendored 文件改为 lakefile 依赖引用；
3. 28.8 万个 Proven 批量模块移入 release artifact 或独立仓库（保留生成器源码与采样校验，供方向一反向蒸馏使用）；
4. 瘦身目标：主仓库 .lean 文件降至 2 万以下；CI 恢复后同步解决 P3（Agda 编译需 Linux ≥16GB）与 P4（36 万文件 OOM）。

### 5.3 papers↔Lean 血缘图谱

把仓库从"两个平行宇宙"变成一个可追溯系统：

- **基础设施**：复用 `framework/SYLVA_DependencyGraph.dot`、知识图谱 JSON 与交叉引用审计的现有设施，缺口仅是两侧从未连线；
- **登记内容**：每个论文系列登记"对应 Lean 模块 / 验证脚本 / theorem 清单"三元组，机器可读（JSON），双向可查（paper→module 与 module→paper）；
- **CI 校验**：断链、孤儿模块（无论文对应）、孤儿论文（无形式化对应）自动报告——孤儿率本身即是方向二知识超图的输入信号；
- **优先顺序**：qec_ftqc、p-adic Hodge、millennium_papers 三个深系列先行打样。

---

## 附：本文件输入清单

1. 全库综合分析报告（2026-08-06，工作区 `TOE-SYLVA_仓库综合分析报告.md`）：目录统计、git 历史、千份抽样、治理文档精读；
2. 千界花园专家组评议：《数学基础强化方案评议》（committee，4 专家）、《新发展方向论证评议》（debate，4 专家）（`qianjie_outputs/panel_数学基础强化.md`、`panel_新发展方向.md`）；
3. arXiv 自动形式化文献扫描（2026-08-06，`qianjie_outputs/arxiv_自动形式化扫描.md`）：12 条 2024–2026 文献，编号 12/12 经核验有效。

> 本文件所述全部数据均来自上述输入与仓库内可核查文件，未新增任何未核验数字。后续每季度按 5.1 口径复盘一次，修订本文件。
